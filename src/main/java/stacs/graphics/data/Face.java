package stacs.graphics.data;

import stacs.graphics.render.Renderable;

public class Face extends Renderable {

    public Face(float[] vertices, float[] colours, int[] indices) {
        super(vertices, colours, indices);
    }
}
